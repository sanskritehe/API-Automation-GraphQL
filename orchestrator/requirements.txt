# Web framework (for the generated app skeleton)
FastAPI>=0.110.0
Pydantic>=2.5.0
uvicorn>=0.27.0

# HTTP client (for API clients + app db_client)
requests==2.32.5

# Environment config
python-dotenv==1.2.2

# OpenAI SDK (replaces GitHub Copilot SDK)
openai>=1.30.0


